format: 1
id: DDD-cost-04
kind: empirical
statement: |
  The 09 §6.2 actor table rows (program, weak model, mid model) are the cost-optimal encodings at three act-volume regimes: which allocation a cost-minimising arrangement selects is set by act volume.
status: projected
region: |
  closing-predicate tasks with a computable encoding frontier; the date task is the worked instance
evidence:
- kind: asset
  ref: core/assets/measure-mdl-demo.py
  note: >-
    the optimal-encoding-vs-volume sweep shows allocation richness monotone in N under both cost
    models; the graded regime sequence exists only under description-length pricing
falsifier: |
  An optimal build-out ordering contradicting observed actor orderings — arrangements at rising volume adopting encodings in an order the cost model rules out.
breaks: |
  Nothing else in core/ — the demand identity and the actor table stand independently of the cost reading.
owner: none
changed: v5.1 (2026-08-08, act-primitive session)
